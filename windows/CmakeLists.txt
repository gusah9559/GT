find_package(OpenGL REQUIRED)
find_package(glfw3 REQUIRED)
find_package(GLEW REQUIRED)

add_library(windows STATIC
    window.cpp
)

target_link_libraries(windows PRIVATE OpenGL::GL glfw GLEW::GLEW)

target_include_directories(windows PRIVATE ${OPENGL_INCLUDE_DIR} ${GLFW_INCLUDE_DIRS} ${GLEW_INCLUDE_DIRS})